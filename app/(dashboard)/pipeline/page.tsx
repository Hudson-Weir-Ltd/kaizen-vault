import PlaceholderPage from "@/components/PlaceholderPage";

export default function PipelinePage() {
  return (
    <PlaceholderPage
      title="Pipeline Ideas"
      description="Standalone view of every proposed AI/automation pipeline with capabilities, projected impact, and status."
      scheduledFor="Stage B follow-up"
    />
  );
}
