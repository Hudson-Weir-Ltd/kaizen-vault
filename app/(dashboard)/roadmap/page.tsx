import PlaceholderPage from "@/components/PlaceholderPage";

export default function RoadmapPage() {
  return (
    <PlaceholderPage
      title="Roadmap"
      description="Phased delivery plan with milestones, KPI targets and dependencies."
      scheduledFor="Stage B follow-up"
    />
  );
}
