import PlaceholderPage from "@/components/PlaceholderPage";

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity"
      description="Full activity feed with filters by event type, service, severity and time window."
      scheduledFor="Stage B follow-up"
    />
  );
}
